# Prismatic Hub Configuration Directory

This directory contains configuration files for the Prismatic Hub:
- `workspaces.yaml`: Default workspaces registry.
- `agents.yaml`: Agent configuration and adapters.
- `router.yaml`: Label/comment-based pipeline routing engine settings.
